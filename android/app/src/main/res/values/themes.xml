<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose supplies all real styling; this base theme just needs to be
         a no-action-bar platform theme so the app launches edge-to-edge. -->
    <style name="Theme.TrueMPG" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/ic_launcher_background</item>
    </style>
</resources>
